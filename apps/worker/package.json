{
  "name": "@link-sentry/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:enqueue-jobs": "node --import tsx src/dev/enqueueJobs.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@link-sentry/db": "*",
    "@link-sentry/crawler": "*"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  }
}
