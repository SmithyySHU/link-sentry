{
  "name": "link-sentry",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:crawler": "npm run -w @link-sentry/crawler dev",
    "dev:db": "npm run -w @link-sentry/db dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "pg": "^8.16.3"
  }
}
